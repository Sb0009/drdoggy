<table size="50%" padding="3" marging="3" border="3" bordercolor="blue">
  <caption><br>Patients<br></caption>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Age</th>
      <th>City</th>
      <th>Contact</th>
      <th>Gender</th>
      <th>Patient Infos</th>

    </tr>
    <% @patients.each do |patient| %>
      <tr>
        <td><%= patient.id %></td>
        <td><%= patient.name %></td>
        <td><%= patient.age %></td>
        <td><%= patient.city %></td>
        <td><%= patient.contact %></td>
        <td><%= patient.gender %></td>
        <td>
          <%= link_to "Edit","/patients/edit/#{patient.id}" %>Edit
          <%= link_to "Delete","/patients/destroy/#{patient.id}" %>Destroy
          <%= link_to "Show", "/patients/#{patient.id}"%>Show
          <%= link_to "All doctors" ,doctors_path(id: patient.id) %> All Doc
        </td>
      </tr>
    <% end %>

</table>
<br>
<br>
<%= link_to "New Patient",new_patient_path %>